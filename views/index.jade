extends layout

block content
  h1= title
  p Welcome to #{title}
  form(method="POST", action="/calculate-coverage")
    input(type="text", name="sequence")
    input(type="text", name="refseq")
    input(type="submit")